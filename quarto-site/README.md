# Quarto redesign prototype

This directory contains the visual skeleton for the next Deep Learning course site.
The existing Jekyll site remains unchanged while the prototype is reviewed.

Preview locally with:

```bash
quarto preview quarto-site
```

Build with:

```bash
quarto render quarto-site
```
