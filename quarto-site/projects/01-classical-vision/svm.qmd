---
title: "Homework 01 · Classical Vision"
subtitle: "Part C — Multiclass SVM and hinge loss"
description: "Implement a vectorized margin loss, derive its gradient, and compare learned class templates."
toc: true
toc-depth: 2
page-layout: article
---

:::{.project-brief}
<span><strong>Duration:</strong> Two sessions × 1h30</span>
<span><strong>Mode:</strong> Individual</span>
<span><strong>Format:</strong> One self-contained Jupyter notebook</span>
<span><strong>Deliverable:</strong> Completed notebook with passing checks, plots, and analysis</span>
:::
## The challenge

The Softmax classifier asks for calibrated relative probabilities. A multiclass linear SVM asks a different question: is the correct score larger than every incorrect score by a safety margin?

$$L_i=\sum_{j\ne y_i}\max(0, s_j-s_{y_i}+\Delta).$$

You will implement this hinge loss without loops, derive the gradient from active margin violations, verify it numerically, and optimize the same linear score function used by Softmax.

```{mermaid}
flowchart LR
    A[Image batch X] --> B[Scores XW + b]
    B --> C[Correct-class score]
    C --> D[Margins max(0, sj - sy + Δ)]
    D --> E[Active-violation gradient]
    E --> F[SGD update]
    F --> B
```

## Open, download, or run

[Open notebook](starter/svm_cifar10.ipynb){.btn .btn-primary}

<a class="btn btn-secondary" href="https://raw.githubusercontent.com/anassBelcaid/deeplearning/redesign/quarto-2026/quarto-site/projects/01-classical-vision/starter/svm_cifar10.ipynb" download="svm_cifar10.ipynb">Download notebook</a>

[Open in Google Colab](https://colab.research.google.com/github/anassBelcaid/deeplearning/blob/redesign/quarto-2026/quarto-site/projects/01-classical-vision/starter/svm_cifar10.ipynb){.btn .btn-secondary}

## What you implement

1. Reproducible small-random parameter initialization.
2. Fully vectorized multiclass hinge loss with L2 regularization.
3. Analytical gradients obtained from the active-margin mask.
4. One in-place SGD update—the backward step that changes the parameters.

The notebook supplies prediction, mini-batch training, numerical gradient checking, the learning-rate/L2 grid search, final evaluation, and visualization of the ten learned weight templates.

## Two-session rhythm

| Session | Work | Checkpoint |
|---|---|---|
| **A · Margins and derivatives** | initialize, score, inspect margins, vectorize loss, derive the active-mask gradient | Checks 1–3 and numerical gradient check pass |
| **B · Optimize and compare** | SGD update, short diagnostic, hyperparameter grid, final test, Softmax/SVM comparison | Check 4 passes; plots and analysis complete |

## Submission standard

Submit the notebook after restarting and running all cells. It must contain all green checks, the numerical-gradient report, loss curves, validation grid, final accuracies, class templates, and completed reflection prompts.

## References

- [CS231n linear classification](https://cs231n.github.io/linear-classify/) — multiclass SVM scores, margins, and vectorized loss.
- [CS231n optimization](https://cs231n.github.io/optimization-1/) — analytical and numerical gradients.
- [Crammer and Singer, 2001](https://jmlr.org/papers/v2/crammer01a.html) — multiclass support-vector machines.
